fastapi>=0.111.0
uvicorn[standard]>=0.29.0
shapely>=2.0.4
numpy>=1.26.0
pydantic>=2.7.0
python-dotenv>=1.0.0
pyshp>=2.3.1          # reads the Somerville centerline + pavement + imagery shapefiles
Pillow>=10.0.0        # resizes bundled 360° site photos into fast thumbnails

# Optional — point cloud ingestion (not required for shapely demo)
# laspy>=2.5.0
# open3d>=0.18.0
