# Copy to .env and fill in — never commit .env

# Cyvl API (required for real data)
CYVL_API_KEY=your_team_api_key_here
CYVL_PROJECT_ID=                    # fill after GET /api/v1/projects

# Autodesk Platform Services (optional — shapely fallback used if absent)
APS_CLIENT_ID=
APS_CLIENT_SECRET=
